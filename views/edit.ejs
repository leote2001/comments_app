<!DOCTYPE html>
<html lang="en">

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>

<body>
    <div class="container text-center">
        <header class="text-center">
    <nav>
        <ul class="list-group">
            <li class="list-group-item"><a href="/">Home</a></li>
            <li class="list-group-item"><a href="/profile/">My profile</a></li>
            <li class="list-group-item"><a href="/favorite/get-all/">My favorite users</a></li>
            <li class="list-group-item">
                <form action="/auth/logout" method="post" style="display: inline;"><button class="btn btn-primary" type="submit">Logout</button>
                </form>
            </li>
        </ul>
    </nav>
        </header>
        <main>
    <form action="/edit" method="post">
        <input type="hidden" name="commentId" id="commentId" value="<%= comment._id %>">
        <input type="hidden" name="source" id="source" value="<%= source %>">
            <h2>Edit</h2>
            <div class="mb-3">
                <textarea aria-describedby="commentAdvice" class="form-control" name="content" id="content" placeholder="Write something"><%= oldData && oldData.content ? oldData.content : comment.content %></textarea>
                <div id="commentAdvice" class="form-text">Maximum length: 180 characters.</div>
            </div>
            <p id="p-length"></p>
            <button id="submitBtn" class="btn btn-primary" type="submit">Submit</button>
    </form>
    <% if (errors.length > 0) { %>
<ul class="list-group">
    <% errors.forEach(error => { %>
<li class="list-group-item"><%= error %></li>
    <%})%>
</ul>
    <%}%>
    <button class="btn btn-primary" onclick="window.history.back()">Back</button>
        </main>
        <footer class="text-center text-muted">
<p>Copyright &copy;2025</p>
        </footer>
    </div>
    <script src="/commentLengthEditPage.js"></script>
</body>

</html>