<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
</head>
<body>
    <div class="container text-center">
        <header class="text-center">
    <h1>Register</h1>
        </header>
        <main>
    <form action="/auth/register" method="post">
        <div class="mb-3">
            <label class="form-label" for="username">Username</label>
            <input class="form-control" id="username" name="username" value="<%= oldData ? oldData.username : ''%>" type="text">
        </div>
        <div class="mb-3">
            <label class="form-label" for="email">Email</label>
            <input class="form-control" id="email" name="email" value="<%= oldData ? oldData.email : ''%>" type="email">
        </div>
        <div class="mb-3">
            <label class="form-label" for="password">Password</label>
            <input class="form-control" id="password" name="password" type="password">
        </div>
        <div class="mb-3">
            <label class="form-label" for="confirmPassword">Confirm password</label>
            <input class="form-control" id="confirmPassword" name="confirmPassword" type="password">
        </div>
        <div class="mb-3">
            <label class="form-label" for="sum">How much is <%= num1 %> + <%= num2 %>?</label>
            <input class="form-control" name="sum" id="sum" type="number">
            <input name="result" id="result" type="hidden" value="<%= result %>">
        </div>
        <button class="btn btn-primary" type="submit">Register</button>
        <div class="mt-3 mb-3">
            <p>If you already have an account, <a href="/auth/login"><b>login</b></a>.</p>
        </div>
    </form>
    <% if (errors.length> 0) { %>
        <ul class="list-group">
            <% errors.forEach(error=> { %>
                <li class="list-group-item">
                    <%= error %>
                </li>
                <%})%>
        </ul>
        <%}%>
        </main>
        <footer class="text-center text-muted">
            <p>Copyright &copy;2025</p>
        </footer>
    </div>
</body>
</html>