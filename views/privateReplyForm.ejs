<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reply to <%= receiver.username %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
<div class="container text-center">
    <header class="text-center">
<%- include("./partials/search") %>
    </header>
    <main>
        <form action="/send-private" method="post">
            <input type="hidden" name="receiverId" id="receiverId" value="<%= receiver._id %>">
            <input type="hidden" name="userId" id="userId" value="<%= authUser %>">
            <h2>Send private comment to: <a href="/profile/<%= receiver._id %>"><%= receiver.username %></a></h2>
            <div class="mb-3">
<label class="form-label" for="title">Title</label>
<input autofocus class="form-control" type="text" name="title" id="title" required maxlength="55" value="<%= title %>">
            </div>
            <div class="mb-3">
                <label class="form-label" for="content">Write something...</label>
                <textarea aria-describedby="commentAdvice" class="form-control" name="content" id="content"><%= content %></textarea>
                    <div id="commentAdvice" class="form-text">Maximum length: 180 characters.</div>
            </div>
            <p id="p-length"></p>
            <button id="submitBtn" class="btn btn-primary" type="submit">Reply</button>
        </form>
        <button onclick="window.history.back()" class="btn btn-primary">Back</button>
    </main>
    <footer class="text-center text-muted">
        <p>Developed by <a target="_blank" href="https://leosprogramador.portfolio-ls.online">Leonardo Sales (Leo S
                Programador)</a></p>
        <p>Copyright &copy;2025</p>
    </footer>
</div>
<script src="/searchScript.js"></script>
<script src="/commentLengthEditPage.js"></script>
</body>

</html>